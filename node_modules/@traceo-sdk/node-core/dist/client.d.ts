import { INodeClient, TraceoOptions } from "./types";
export declare abstract class CoreClient implements INodeClient {
    options: TraceoOptions;
    headers: {
        [key: string]: any;
    };
    constructor(apiKey: string, { collectMetrics, offline, ...opts }: Omit<TraceoOptions, "apiKey">);
    private configGlobalClient;
    private get isOffline();
    abstract initSDK(): void;
}
