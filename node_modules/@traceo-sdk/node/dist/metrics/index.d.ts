export declare class MetricsRunner {
    private client;
    private readonly interval;
    private readonly cpuUsage;
    private readonly eventLoop;
    private readonly heap;
    private readonly memoryUsage;
    clientCounterMetrics: Record<string, number>;
    clientGaugeMetrics: Record<string, number>;
    clientTimeSeriesMetrics: Record<string, number>;
    clientMeauserementMetrics: Record<string, number[]>;
    constructor();
    register(): void;
    private collectMetrics;
    private get loadAvg();
}
