/**
 * Logger for bank process
 * @param {any} message
 * @returns {any}
 * @memberof Utils
 * @author land
 * @utils Utils/Logger
 */
export default function log(message: any) {
    console.log(`[ Hazline LOG ] ${message}`);
}
