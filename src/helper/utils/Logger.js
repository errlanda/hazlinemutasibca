/**
 * Logger for bank process
 * @param {any} message
 * @returns {any}
 * @memberof Utils
 * @author land
 * @utils Utils/Logger
 */
// Logger.js

function log(message) {
    console.log(`[ Hazline LOG ] ${message}`);
}

module.exports = log;
